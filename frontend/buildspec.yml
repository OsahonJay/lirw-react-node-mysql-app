version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd frontend
      - npm install

  build:
    commands:
      - cd frontend
      - npm run build

  post_build:
    commands:
      - echo "Frontend build complete"

artifacts:
  base-directory: frontend
  files:
    - appspec.yml
    - scripts/**/*
    - dist/**/*
  discard-paths: no
